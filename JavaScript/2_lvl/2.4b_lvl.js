// Развернуть массив на месте (in-place)
let arr = [1, 2, 3, 4];

function isPalindromeArray(arr) {
  // код
}


const reverseArr = isPalindromeArray(arr);
console.log(`Развернутый массив: ${reverseArr}`);